# Security

Logit Scope is intended to bind to `127.0.0.1`. It has no authentication or transport encryption. Do not expose it to an untrusted network, and treat model output as untrusted text.

To report a security issue privately, contact the repository owner instead of opening a public issue.
